import { Box, Typography } from "@mui/material";

export default function () {
  return (
    <Box className="appbarheader" m={1} mb={0}>
      <Typography className="app-bar-header-title">Admin UI</Typography>
    </Box>
  );
}
